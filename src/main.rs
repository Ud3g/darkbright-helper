use darkbright_helper::{BrightnessError, Result};

fn main() {
    println!("Brightness Control Tool Starting...");

    // Temporär: Typen verwenden um Warnungen zu vermeiden
    let _: Option<Result<()>> = None;
    let _: Option<BrightnessError> = None;
}
